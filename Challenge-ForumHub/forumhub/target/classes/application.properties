# Configura��es do banco de dados MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/forumhub?useSSL=false&serverTimezone=UTC
spring.datasource.username=seu_usuario
spring.datasource.password=sua_senha
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA (Java Persistence API)
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true

# Flyway
spring.flyway.enabled=true
spring.flyway.baseline-on-migrate=true

# Configura��es do JWT (para etapas futuras)
jwt.secret=secreta-chave
jwt.expiration=3600000

forumhub.jwt.secret=seuSegredoSuperSecreto
forumhub.jwt.expiration=86400000 # 24 horas em milissegundos

